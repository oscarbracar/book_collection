<p id="notice"><%= notice %></p>

<h1>User Books</h1>

<table>
  <thead>
    <tr>
      <th style="text-align: left; padding-right: 20px;">Username</th>
      <th style="text-align: left; padding-right: 20px;">Book Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_books.each do |user_book| %>
      <tr>
        <td style="padding-right: 20px;"><%= user_book.user.username %></td>
        <td style="padding-right: 20px;"><%= user_book.book.title %></td>
        <td style="padding-right: 10px;"><%= link_to 'Show', user_book %></td>
        <td style="padding-right: 10px;"><%= link_to 'Edit', edit_user_book_path(user_book) %></td>
        <td><%= link_to 'Destroy', user_book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Book', new_user_book_path %><br>
<%= link_to 'Book index', books_path %><br>
<%= link_to 'User index', users_path %>
